# WASM 提交前验证 · 2026-09-07

原型代码已整理到正式 Rust workspace；原生 CLI 仍为 default-members。
单独的 `assfonts-wasm` crate 通过纯内存 ABI 复用既有 Processor/HarfBuzz，
构建脚本使用固定日期工具链，不再依赖临时原型的目录布局。

## 本次实际执行

- `scripts/setup_wasm.py` 复用已校验 revision 的 Emsdk 4.0.23，实际安装
  `nightly-2025-12-17` 命名工具链及 rust-src；没有修改默认 Rust 或 shell 启动文件。
- `scripts/build_wasm.py` 在此前不存在的 `target/emscripten/` 完整构建，约 59 秒。
  rustc 为 `1.94.0-nightly (31010ca61 2025-12-16)`，Emscripten 为 4.0.23。
- 新生成的 WASM：1,803,941 字节，SHA-256
  `6de8cfe14d27a2980a528e7d50b50b9971053383ff23f3980977264dcb137b5f`。
  JS 加载器：10,962 字节，SHA-256
  `409a120d54090d57c050303e53e47823bde94d34e4c95a7bb43677fdc26bf9d0`。
- 在本机 Deno 2.9.6 和服务器 Linux x86_64 Deno 2.5.6 对这份产物执行验证：
  禁止子进程和网络，没有 `--no-check`。类型检查和 10 项 WASM 检查均通过，
  包括强制 memory.grow、错误后恢复、字体副本所有权和关闭后拒绝。
- 子集哈希与原生基准相同；TTC 使用 face 1，CFF 使用 face 0。
- 本机 libass 0.17.5 比较 50 个时刻，47 个非空，RGBA 零差异，附件移除负对照通过。
  这是本次新产物的渲染验证；没有声称重新运行原型的全部真实字幕样本。
- Rust 1.92.0 的完整 workspace 22 个测试、fmt、Clippy `-D warnings`、禁用原生
  后端检查通过。WASM ABI 的 2 个测试也在原生平台验证错误与内存合同。
- 两份 CI workflow 通过 Actionlint 1.7.7；未启用 ShellCheck。

## 限制与提交范围

内存常驻稳定性仍待解决，具体原型观察见 [WASM 测试说明](../../tests/wasm/README.md)。
这次提交源码、固定工具链配置、构建入口、独立测试和 CI，不提交 SDK、生成二进制、
商业字体、真实剧集字幕，也不替换 ass-processor-lib 或 NAS 正式文件。

仓库没有远程地址，没有推送或触发 GitHub Actions。实际验证来自本机构建和服务器
Deno 执行；不声称 Ubuntu 的远程 CI 或 Windows Emscripten 构建已经通过。
详细本地输出留在忽略的 `.validation/wasm-*` 和 `target/wasm/`。
